# `.claude/rules/` — Claude Code rule supplements

Markdown files here extend baseline instructions for **Claude Code**. They do **not** replace root
**`AGENTS.md`**, root **`CLAUDE.md`**, or **`.cursor/rules/`**; read those together.

| File | Purpose |
| --- | --- |
| [`source-material-intake.md`](source-material-intake.md) | Safe use of optional `source-material/` (transformative rewrite only). |
| [`../CLAUDE.md`](../CLAUDE.md) | Short supplement; defers to root `CLAUDE.md` and `AGENTS.md`. |

There is no separate runnable “source-material agent” in this repo. Intake policy is
**repository-wide**.
